import React from "react";
import { SwitchProps } from "react-router";
import { RouteConfig } from "./types";
declare function renderRoutes(routes: RouteConfig[], extraProps?: any | undefined, switchProps?: SwitchProps | undefined): React.ReactNode;
export default renderRoutes;
